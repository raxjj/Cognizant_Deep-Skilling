# Application configuration file
# Spring Boot auto-loads this at startup and merges values into the Environment.

spring.application.name=spring-learn

# Example: change the default embedded Tomcat port (default is 8080)
# server.port=8081

# Example: set root logging level
logging.level.root=INFO
logging.level.com.cognizant.springlearn=DEBUG
